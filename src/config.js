const config = {
    basename: '/easemarketing',
    defaultPath: '/login',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12,
    theme: 'light',
    i18n: 'en', 
};

export default config;
